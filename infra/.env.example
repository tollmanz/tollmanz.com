# Local secrets for `npm run preview` / `npm run up`. Never committed (.env is gitignored).
# In CI these come from GitHub Actions secrets instead.

# Fastly API token with write access to the service
FASTLY_API_KEY=
